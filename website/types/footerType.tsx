export type socialType = {
    image: string;
    link: string;
};

export type photoType = {
    name: string;
    link: string;
};
export type companyType = {
    name: string;
    link: string;
};
