export type compareType = {
    label: string;
    studio: string;
    insta: string;
};

export type compareReviewType = {
    name: string;
    comment: string;
    image: string;
    star: string;
};
